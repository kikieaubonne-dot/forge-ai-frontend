import React from "react";
import ReactDOM from "react-dom/client";
import ForgeAI from "./ForgeAI.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ForgeAI />
  </React.StrictMode>
);
